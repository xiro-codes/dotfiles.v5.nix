# Kitty

Kitty terminal emulator with custom configuration.

## Options

### `local.kitty.enable`

*   **Type:** `Boolean`
*   **Default:** `false`
*   **Description:** Enables the Kitty terminal emulator. Sets the terminal variable to kitty and adds extra configuration such as `window_padding_width`.
